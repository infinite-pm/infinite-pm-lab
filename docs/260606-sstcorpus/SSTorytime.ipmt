c1::a "$ sudo su -\n$ su - postgres\n$ psql\n# CREATE user sstoryline password 'sst_1234' superuser;\n# CREATE DATABASE sstoryline;\n# CREATE DATABASE newdb;\n# GRANT ALL PRIVILEGES ON DATABASE sstoryline TO sstoryline;\n# GRANT ALL PRIVILEGES ON DATABASE newdb TO sstoryline;\n# CREATE EXTENSION UNACCENT;" ::c
t2::a "%\"Longer phrases\" used a limited number of times are common concepts" ::t "%\"Longer phrases\" are easier to write than short precise phrases, so people need help to break them down and summarize their intent" ::tip
t3::a "%\"Short phrases\" used once are usually unique names i.e. a %\"proper name\"" ::t
t4::a "%\"n-grams\"" ::t
c5::a %factoids and partial %knowledge ::c
e6::a "A graph is basically a structure with an index at every node. There are two modes of use: random access lookup for starting a search--which SQL is already good at, and exploring, path tracing, following leads from that starting point like hypertext." ::e
t7::a A recursive search in SQL ::t
c8::a A unique key for a type of link arrow and its properties ::c
t9::a ArrowPtr ::t
e10::a "Because database connection is slow, we do as much as possible to reduce that amount of data sent over the wire by performing all processing internally." ::e "This has the tradeoff of using a slow script language in the database kernel." ::tip
c11::a "Comprehensive search by %\"NCCS criteria\"" ::c
e12::a "Context has two roles in knowledge: promise theoretically it has a '+' donor role when categorizing information through notes. It also has a \"-\" acceptance or filtering role when searching. Both of these are represented by the \":: ... ::\" lines in N4L." ::e
t13::a "CreateDBNode(ctx PoSST, n Node) Node" ::t
t14::a "CreateDBNodeArrowNode(ctx PoSST, org NodePtr, dst Link, sttype int) bool" ::t
c15::a "FUNCTION SumFwdPaths(start link, path text, orientation text, depth integer, maxdepth integer, exclude nodeptr[])\n DECLARE\n     fwdlinks Link[];\n     empty Link[] = ARRAY[]::Link[];\n     lnk Link;\n     fwd Link;\n     ret_paths Text;\n     appendix Text;\n     tot_path Text;\n BEGIN\n IF depth = maxdepth THEN\n   ret_paths := Format('%s\\n%s',ret_paths,path);\n   RETURN ret_paths;\n END IF;\n fwdlinks := GetFwdLinks(start.Dst,exclude,sttype);\n FOREACH lnk IN ARRAY fwdlinks LOOP\n    IF NOT lnk.Dst = ANY(exclude) THEN\n       exclude = array_append(exclude,lnk.Dst);\n       IF lnk IS NULL THEN\n         ret_paths := Format('%s\\n%s',ret_paths,path);\n          RETURN ret_paths;\n      ELSE\n          tot_path := Format('%s;%s',path,lnk::Text);\n          appendix := SumFwdPaths(lnk,tot_path,sttype,depth+1,maxdepth,exclude);\n          IF appendix IS NOT NULL THEN\n             ret_paths := Format('%s\\n%s',ret_paths,appendix);\n          ELSE\n            ret_paths := Format('%s\\n%s',ret_paths,tot_path);\n         END IF;\n      END IF;   \nEND IF;\nEND LOOP;\nRETURN ret_paths;\nEND;" ::c
c16::a Fact based cramming ::c
c17::a Forensic fact assimilation and reasoning. ::c
t18::a "GetDBArrowByName(ctx PoSST,name string) ArrowPtr" ::t
t19::a "GetDBArrowByPtr(ctx PoSST,arrowptr ArrowPtr) ArrowDirectory" ::t
t20::a "GetDBArrowBySTType(ctx PoSST,sttype int) []ArrowDirectory" ::t
t21::a "GetDBArrowsMatchingArrowName(ctx PoSST,s string) []ArrowPtr" ::t
t22::a "GetDBArrowsWithArrowName(ctx PoSST,s string) (ArrowPtr,int)" ::t
t23::a "GetDBChaptersMatchingName(ctx PoSST,src string) []string" ::t
t24::a "GetDBContextByName(ctx PoSST,src string) (string,ContextPtr)" ::t
t25::a "GetDBContextByPtr(ctx PoSST,ptr ContextPtr) (string,ContextPtr)" ::t
t26::a "GetDBNodePtrMatchingNCCS(ctx PoSST,nm,chap string,cn []string,arrow []ArrowPtr,seq bool,limit int) []NodePtr" ::t
t27::a "GetDBNodePtrMatchingName(ctx PoSST,name,chap string) []NodePtr" ::t
t28::a "GetDBPageMap(ctx PoSST,chap string,cn []string,page int) []PageMap" ::t
t29::a "GetDBSingletonBySTType(ctx PoSST,sttypes []int,chap string,cn []string) ([]NodePtr,[]NodePtr)" ::t
t30::a "GetEntireNCSuperConePathsAsLinks(ctx PoSST,orientation string,start []NodePtr,depth int,chapter string,context []string,limit int) ([][]Link,int)" ::t
t31::a "GetFwdConeAsNodes(ctx PoSST, start NodePtr, sttype,depth int,limit int) []NodePtr" ::t
t32::a "GetFwdPathsAsLinks(ctx PoSST, start NodePtr, sttype,depth int, maxlimit int) ([][]Link,int)" ::t
t33::a GetSTtypesFromArrows(arrows []ArrowPtr) []int ::t
e34::a "Graphs are for following paths with different meanings - or \"semantics\"." ::e
c35::a HOW-TO sequences and recipes ::c
t36::a "IdempDBAddLink(ctx PoSST,from Node,link Link,to Node)" ::t
t37::a "IdempDBAddNode(ctx PoSST,n Node) Node" ::t
c38::a Incident reporting with causal commentary ::c
t39::a "Knowledge scanned from text is most likely to be in the form of paragraphs and sentences. It needs to be broken down into its constituent parts, like a DNA analysis, to understand its relationship to other notes." ::t
c40::a Language learning ::c
t41::a Link ::t
e42::a "Loading data into a database, with validation, is slow. Retrieving is much faster." ::e
t43::a Making %notes ::t "Notes are more concise than documents, prose, or spoken language. · Notes are condensed intent without flowery elaboration. · AI and LLMs produce fluid %prose language, which is not in note form." ::tip
e44::a "Most graph databases don't give any guidance in modelling or searching. They are based on SQL random access search, but this is not how we need to use graphs." ::e
t45::a NCCS criteria ::t
t46::a Node ::t
c47::a "Node, Chapter, Context, Sequence: searches will select nodes that match all these criteria in one go" ::c
t48::a NodePtr ::t
c49::a PLpgSQL ::c
t50::a PageMap ::t
t51::a PoSST ::t
c52::a Promise Theory ::c
t53::a "Ranking of n-grams is based on two parts: the intentionality or amount of work needed to write them, and the frequency of use" ::t
c54::a Reasoning with mathematical support ::c
c55::a Risk procedure documentation ::c
c56::a SRE problem diagnosis methods ::c
e57::a SSTorytime ::e "made with Linux but doesn't depend on anything specific to Linux" ::tip
e58::a "SSTorytime shows you the ambient time in a coarse grained form after every search, so that you can copy and paste this into context tags if you want to be able to use it later. If you make a search without any search terms, you will see any items that are labelled in a matching time. So, if you have specific \"to-do\"/todo / to do lists for specific times, these will pop up by themselves." ::e
e59::a "SSTorytime takes a \"Promise Theory\" approach." ::e
t60::a "SelectStoriesByArrow(ctx PoSST,nodeptrs []NodePtr, arrowptrs []ArrowPtr, sttypes []int, limit int) []NodePtr" ::t
t61::a "SolveNodePtrs(ctx PoSST,nodenames []string,search SearchParameters,arr []ArrowPtr,limit int) []NodePtr" ::t
e62::a "Sometimes you might see chapters that you didn't select showing up in searches. This is probably a result of certain words and phrases belonging to more than one chapter, and thus bridging chapters that you didn't intend. This possibility for bridging is intentional, as it allows \"lateral thinking\", which is an important source of discovery." ::e
t63::a THEME_LIM ::t
e64::a The goal is to implement useful search patterns for a graph structure in an efficient way. ::e
e65::a The hard problem of knowledge is context. ::e
t66::a "The most frequently used n-grams are the least meaningful, used for padding and rhythm" ::t
e67::a "The overlap between \"+\" and \"-\" promises is the outcome. In order to make this effective, it requires some planning in the way we name sections. This is something that develops over time, so we have to understand that knowledge is individual, dynamic and interactive--a process, not a stale archive written once and for all." ::e
t68::a "UploadArrowToDB(ctx PoSST,arrow ArrowPtr)" ::t
t69::a "UploadInverseArrowToDB(ctx PoSST,arrow ArrowPtr)" ::t
t70::a "UploadNodeToDB(ctx PoSST, org Node)" ::t
t71::a "UploadPageMapEvent(ctx PoSST, line PageMap)" ::t
e72::a "What role should times play? Precise times are irrelevant because they never repeat. Coarse grains of time like \"Monday morning\" do repeat and may be relevant in finding intended information. But how do we record the time? Is it the time at which information" ::e
e73::a "When we are laying out new information, we use the context tags/tokens like a section heading. This is the \"+\" promise." ::e
e74::a "When we are searching, we use context tags as a filter to reduce the scope of a search. This is the \"-\" promise." ::e
e75::a "While engaged in a search process, the knowledge agent remembers the intended parts of searches as current scenario context. You will see some of this history pop up at the top of the search result to give you further hints based on past behaviour." ::e
t76::a "You cam limit the number of entries returned using the LIMIT keyword. If you make this number large, the query will take a long time to return, especially over the web, as the marshalling cost is high." ::t
t77::a "You can select colour themes in the web browser by typing \\theme and a colour style. Currently supported: light, dark, slate, spaceblue/astronomy/blue and red." ::t
t78::a "You can start broad with general search terms, without specifying specific chapters. If you have a lot of data, this becomes less and less likely to succeed. As data grow, we need to narrow the search and we can't use popularity voting like PageRank to guess which are the important results." ::t
t79::a "You can start narrow with a very specific term by quoting longer strings including spaces, but this assumes you know what is already there. Then you can gradually reduce the number of constraints by ADDING contexts and search terms." ::t
c80::a \( x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a} \) ::c
t81::a a %paragraph is defined as either a text measure separated by two newlines or if we exceed the the maximum threshold %THEME_LIM ::t
c82::a "a collection of signals that identify a scenario, like a scene description language" ::c
c83::a a constant scale approximately the size of a long paragraph set to 100 sentences ::c
c84::a a different grammatical form of the same word of phrase ::c
c85::a a measure of the degree to which something might be intended ::c
c86::a a measure that distinguishes ad hoc or random change from %causal %change ::c
c87::a a nugget of meaningful information contributing to knowledge ::c
t88::a a text source of document ::t
c89::a adding a NodeArrowNode secondary/derived structure to postgres ::c
c90::a an ad hoc identifier without descriptive meaning ::c
c91::a appending a node when you don't want to manage the NPtr values. ::c
t92::a causal ::t
t93::a causality ::t
t94::a context ::t
e95::a create the SST database ::e
t96::a documents can be scanned to identify %context and %factoid parts ::t
c97::a entry point for adding a link to a node in postgres ::c
c98::a establishing a connection to the SST library service ::c
c99::a establishing a node in postgres without auto %NPtr assignment ::c
t100::a factoid ::t
c101::a find nodes that are sources or sinks for a specific STType ::c
c102::a finding a list of %NPtr matching a substring by name ::c
c103::a finding a set of matching NPtrs satisfying the search parameters compiled by a search command ::c
c104::a finding nodes that are sources for story sequences matching the arrow types ::c
c105::a fragment of n words as an ordered sequence ::c "any sentence can be broken up into n-gram fragments, like a DNA analysis." ::tip
e106::a go programming language ::e
t107::a host    all             all             127.0.0.1/32            password ::t
t108::a "host    all             all             ::1/128                 password\"\n\n # to finish ...\n\n\n\n\n stored procedures/functions in postgres\n\n    \"" ::t "uses a language that looks a lot like SQL but is markedly different - beware!" ::tip
c109::a intent ::c
c110::a intentionality ::c
c111::a lateral thinking ::c
t112::a local   all             all                                     peer ::t
c113::a measuring intent ::c
t114::a n-gram ::t
t115::a n-grams ::t
c116::a n-grams that occur less than ::c
t117::a noise level ::t
c118::a obtaining a list of arrowpointers matching the approximate name ::c
c119::a obtaining a list of chapters matching by name ::c
c120::a obtaining an arrowpointer and STType matching a precise name ::c
c121::a obtaining an arrowpointer for precise arrowname - redundant ::c
c122::a obtaining context sets that match by string name ::c
c123::a obtaining the context set with given index pointer ::c
c124::a obtaining the generic semantic spacetime type for a given a list of arrow pointers ::c
c125::a obtains a page from the named chapter as a page map ::c
c126::a obtains all possible paths from a node along STtype links ::c
c127::a "obtains all possible paths from a starting node, with orientation \"fwd,bwd,any\" as link arrays matching the chapter and context criteria" ::c
c128::a obtains an arrow directory entry for a given arrow pointer ::c
c129::a obtains the arrow directory for a given STtype ::c
c130::a obtains the orbit around a starting node as a set of nodes to given depth ::c
e131::a open for local connections ::e
t132::a paragraphs ::t
e133::a postgres ::e
e134::a postgres configuration ::e
e135::a postgres_contrib package for unaccent() function ::e
t136::a proper name ::t
t137::a punctuated fragments ::t
c138::a ranking the %intentionality or importance of text ::c
c139::a "representing a graph link, with arrow and destination node and weight" ::c
c140::a representing core aspects of a single graph node ::c
c141::a representing the original N4L intended layout of notes ::c
t142::a sentences ::t
c143::a supports all human languages in Unicode ::c "languages that don't use spaces between words may have some issues" ::tip
c144::a to-do ::c
c145::a todo ::c
c146::a "type Link struct {  // A link is a type of arrow, with context\n                    // and maybe with a weightfor package math\n\tArr ArrowPtr         // type of arrow, presorted\n\tWgt float32          // numerical weight of this link\n\tCtx ContextPtr       // context for this pathway\n\tDst NodePtr          // adjacent event/item/node\n}" ::c
c147::a "type Node struct {\n\n\tL         int     // length of text string\n\tS         string  // text string itself\n\n\tSeq       bool    // true if this node begins an intended sequence, otherwise ambiguous\n\tChap      string  // section/chapter name in which this was added\n\tNPtr      NodePtr // Pointer to self index\n\n\tI [ST_TOP][]Link  // link incidence list, by STindex - these are the \"vectors\" +/-\n  \t                  // NOTE: carefully how STindex offsets represent negative SSTtypes\n}" ::c
c148::a "type NodePtr struct {\n\n\tClass int            // Text size-class, used mainly in memory\n\tCPtr  ClassedNodePtr // index of within name class lane\n}" ::c
c149::a "type PageMap struct {  // Thereis additional intent in the layout\n\n\tChapter string\n\tAlias   string\n\tContext ContextPtr\n\tLine    int\n\tPath    []Link\n}" ::c
c150::a unique key referring to a node and pointing to its data ::c
c151::a uploading a PageMap structure from memory to postgres ::c
c152::a uploading an arrow definition from memory to postgres ::c
c153::a uploading an existing Node in memory to postgres ::c
c154::a uploading an inverse arrow definition ::c
t155::a uses a language that looks a lot like SQL but is markedly different - beware! ::t
t156::a variant ::t
e157::a "was written, uploaded, or when something actually happened? The technology can't guess this for you, because it needs to be intentional. The ambient time isn't necessarily relevant." ::e
c158::a work ::c

e57 --::X "has feature"--> c143
e133 --::L "sustains"--> e57
c110 --::N "is a variant of"-- c109
c158 --::X "used for"--> c113
t63 --::X "defined as"--> c83
c109 --::X "means"--> c86
c109 --::X "defined as"--> c85
t156 --::X "means"--> c84
t92 --::N "is a variant of"-- t93
t114 --::N "is a variant of"-- t115
t114 --::X "means"--> c105
t94 --::X "means"--> c82
t100 --::X "means"--> c87
t100 --::X "has example"--> c5
t142 --::P--> t132
t137 --::P--> t142
t142 --::X "used for"--> c5
t51 --::X "used for"--> c98
t46 --::X "used for"--> c140
t46 --::X "defined as"--> c147
t48 --::X "used for"--> c150
t48 --::X "defined as"--> c148
t41 --::X "used for"--> c139
t41 --::X "defined as"--> c146
t9 --::X "used for"--> c8
t50 --::X "used for"--> c141
t50 --::X "defined as"--> c149
t45 --::X "means"--> c47
t117 --::X "defined as"--> c116
t136 --::X "means"--> c90
t4 --::P--> t137
t137 --::X "used for"--> c138
t43 --::X "might be used for"--> c38
t43 --::X "might be used for"--> c55
t43 --::X "might be used for"--> c56
t43 --::X "might be used for"--> c35
t43 --::X "might be used for"--> c40
t43 --::X "might be used for"--> c16
t43 --::X "might be used for"--> c17
t43 --::X "might be used for"--> c54
e134 --::L "sustains"--> e133
e106 --::L "sustains"--> e57
t68 --::X "used for"--> c152
t69 --::X "used for"--> c154
t33 --::X "used for"--> c124
e135 --::L "sustains"--> e57
e64 --::L "then followed by"--> e10
e42 --::L "then followed by"--> e6
e65 --::L "then followed by"--> e59
e59 --::L "then followed by"--> e12
e59 --::X "refers to"--> e57
e59 --::X "refers to"--> c52
e73 --::L "then followed by"--> e74
e74 --::L "then followed by"--> e67
t132 --::P--> t88
c54 --::X "is expressed in formula"--> c80
e95 --::L "sustains"--> e133
e95 --::X "has example"--> c1
e131 --::L "sustains"--> e133
e131 --::X "has example"--> c1
t155 --::X "has name"--> c49
t7 --::X "has example"--> c15
t13 --::X "used for"--> c99
t70 --::X "used for"--> c153
t71 --::X "used for"--> c151
t37 --::X "used for"--> c91
t36 --::X "used for"--> c97
t14 --::X "used for"--> c89
t27 --::X "used for"--> c102
t26 --::X "used for"--> c11
t23 --::X "used for"--> c119
t24 --::X "used for"--> c122
t25 --::X "used for"--> c123
t29 --::X "used for"--> c101
t60 --::X "used for"--> c104
t22 --::X "used for"--> c120
t21 --::X "used for"--> c118
t18 --::X "used for"--> c121
t19 --::X "used for"--> c128
t20 --::X "used for"--> c129
t28 --::X "used for"--> c125
t31 --::X "used for"--> c130
t32 --::X "used for"--> c126
t61 --::X "used for"--> c103
e10 --::L "then followed by"--> e42
e6 --::L "then followed by"--> e44
e44 --::L "then followed by"--> e34
e12 --::L "then followed by"--> e73
e67 --::L "then followed by"--> e75
e75 --::L "then followed by"--> e62
e62 --::L "then followed by"--> e72
e62 --::X "refers to"--> c111
e72 --::L "then followed by"--> e157
e157 --::L "then followed by"--> e58
e58 --::X "refers to"--> c144
e58 --::X "refers to"--> c145
t53 --::X "refers to"--> c110
t53 --::X "refers to"--> c158
t108 --::X "has example"--> c15
t30 --::X "used for"--> c127
